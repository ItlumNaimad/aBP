spring:
  application:
    name: backend
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/abpdb
    username: abpuser
    password: abppassword
  sql:
    init:
      mode: always

gemini:
  api-key: ${GEMINI_API_KEY:mock}
